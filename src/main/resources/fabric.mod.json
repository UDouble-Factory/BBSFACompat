{
  "schemaVersion": 1,
  "id": "bbsfacompat",
  "version": "${version}",
  "name": "BBSFACompat",
  "description": "FA Player resource pack animations in BBS, with editable animation layers.",
  "authors": [],
  "contact": {},
  "license": "MIT",
  "environment": "client",
  "mixins": [
    "bbsfacompat.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric-api": "*",
    "minecraft": "${minecraft_version}",
    "java": ">=21",
    "bbs": "=${bbs_version}"
  }
}
